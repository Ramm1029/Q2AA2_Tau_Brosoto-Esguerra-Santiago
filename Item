package game;

public class Item implements Descriptive {
    private String name, description;
    private int durability, damage, range;
    private boolean pickedUp;

    public Item(String name, String description, int durability, int damage, int range) {
        this.name = name;
        this.description = description;
        this.durability = durability;
        this.damage = damage;
        this.range = range;
        pickedUp = false; 
    }
    // use Item to attack Killer
    public void use(Killer killer, int distance) {
        if (durability > 0) {
            if (distance <= range) {
                killer.takeDamage(damage);
                decreaseDurability();
            }
        } else {
            System.out.println(this.name + " is broken and cannot be used.");
        }
    }
   public void performFunction(Fixture fixture) {
        if (durability > 0) {
            fixture.performFunction();
            decreaseDurability();
            System.out.println(this.name + " performed its function on " + fixture.getName());
        } else {
            System.out.println(this.name + " is broken and cannot be used.");
        }
    }
    public void decreaseDurability() {
        durability--;
    }
    public boolean isBroken() {
        return durability <= 0;
    }
    public boolean isPickedUp() {
        return pickedUp;
    }

    @Override
    public void showDescription() {
        System.out.println("Item: " + name + "\nDescription: " + description);
    }
}
